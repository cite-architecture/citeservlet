citeservlet
===========

The CITE services suite implemented using a SPARQL endpoint for source data

## N.b. CSS

You will want to clone the [hmt-core-css](https://github.com/Eumaeus/hmt-css-core), and copy the file `hmt-core.css` from that repository into the Cite Servlet repository at: `.../src/main/webapp/css/`.
